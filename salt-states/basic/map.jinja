{% set map = salt['grains.filter_by']({
    'Debian': {
        'pkgs': [
            'tmux',
            'sudo',
            'iotop',
            'htop',
            'iftop',
            'git',
        ],
    },
    'RedHat': {
        'pkgs': [
            'tmux',
        ],
    },
}, merge=salt['pillar.get']('basic:lookup')) %}
